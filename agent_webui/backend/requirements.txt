# 网关后端依赖（安装进 agent_webui/venv-gateway，不进项目根 venv）
# 用法见 agent_webui/README.md
fastapi>=0.115
uvicorn>=0.30
# backend/api.py 直接 import pydantic（虽然 fastapi 会带进来，仍显式声明）
pydantic>=2.9
# 网关进程内要读 YAML：backend/config.py 读 config.yaml，
# backend/mcp_view.py 会 import agent 侧的 mcp_station（读 STATION.md 的 frontmatter）。
# 缺了它 MCP 面板会整体报「MCP 模块不可用：No module named 'yaml'」。
pyyaml>=6.0
