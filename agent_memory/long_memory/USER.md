# USER.md — 关于你的主人

<!-- ONBOARDING: PENDING -->

> **本文件当前是模板。** 首次启动引导时把主人的画像填进下面各节，然后删掉本段与顶部标记。
> 填写规范见 `AGENTS.md` § 首次启动引导。
>
> ⚠️ **注意：本文件当前版本不会被注入系统提示**（注入的只有 `SOUL.md` / `AGENTS.md` / `MEMORY.md`，
> 见 `agent/agent.py` 的 `load_system_prompt()`）。它是一份**档案**：需要时由你自己调用文件工具读取。
> 想让某些信息每轮都生效，请写进 `AGENTS.md` 或 `SOUL.md`。

## 基本

- **称呼：** `待填`（主人希望你怎么叫他）
- **代词：** `待填`
- **时区：** `待填`（影响时间类工具与"今天/明天"的判断）
- **语言：** `待填`（默认用这个语言回复）
- **称呼我（agent）为：** `待填`

## 技术背景

- **语言 / 框架：** `待填`
- **熟练度：** `待填`（决定给方案时要不要解释基础概念）
- **常用环境 / 工具：** `待填`（操作系统、编辑器、常用 CLI）

## 偏好

- **沟通风格：** `待填`（简洁 / 详细 / 先给结论）
- **交付标准：** `待填`（例如"必须真跑验证，不接受只讲思路"）
- **反感的事：** `待填`（例如"凭空编造""报喜不报忧"）

## 边界

- **可以自己决定的：** `待填`
- **必须先问的：** `待填`

## Context

_（随着了解加深逐步完善）_
